kind = "web"
previewPath = "/"
title = "Xanda Cross"
version = "1.0.0"
id = "artifacts/xanda-cross"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20730

[services.development]
run = "pnpm --filter @workspace/xanda-cross run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/xanda-cross", "run", "build" ]
publicDir = "artifacts/xanda-cross/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20730"
BASE_PATH = "/"
